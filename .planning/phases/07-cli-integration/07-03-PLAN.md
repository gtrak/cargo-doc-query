---
phase: 07-cli-integration
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - src/main.rs
  - src/cli/expand.rs
  - src/types/filter.rs
autonomous: true

must_haves:
  truths:
    - "--include and --only are mutually exclusive (error if both)"
    - "Conflicting filters detected and reported with helpful message"
    - "FILTERING section in --help with 3-4 real examples"
    - "--help-filters flag shows detailed glob syntax guide"
    - "Invalid patterns show what was wrong + valid example + glob reference"
    - "Case-insensitive kind matching works (function, Function, FN)"
  artifacts:
    - path: "src/main.rs"
      provides: "--help-filters subcommand or flag"
      min_lines: 30
    - path: "src/cli/expand.rs"
      provides: "Filter validation logic"
      min_lines: 40
    - path: "src/types/filter.rs"
      provides: "Enhanced help text and validation methods"
      min_lines: 20
  key_links:
    - from: "src/cli/expand.rs"
      to: "src/types/filter.rs"
      via: "FilterEngine::validate_patterns and glob_syntax_help"
      pattern: "validation and help integration"
---

<objective>
Add validation, help text, and error handling for filter flags.

Purpose: Provide clear documentation, validate flag combinations, and give helpful error messages when users provide invalid input.

Output: Comprehensive help text, validation logic, and user-friendly error messages for all filter scenarios.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-cli-integration/07-CONTEXT.md
@src/main.rs
@src/cli/expand.rs
@src/types/filter.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation for conflicting filter flags</name>
  <files>src/cli/expand.rs</files>
  <action>
    Implement validation logic in ExpandCommand to detect conflicts:

    1. Add a validate() method to ExpandCommand that runs before execute():
       fn validate(&self) -> anyhow::Result<()>

    2. Check for --include and --only both being specified:
       - If self.only.is_some() && !self.include.is_empty():
         - Return error: "Cannot use --include with --only. --only is shorthand for 'include this and exclude everything else'"

    3. Check for contradictory filters (if easy to detect):
       - Same pattern in include and exclude: warning (not error, since exclude wins)
       - Completely contradictory patterns: error with "Filter patterns have no overlap — review your patterns"
       - Example: --include "std::*" --exclude "std::*" should error

    4. Validate kind values (case-insensitive, but warn if unknown):
       - Accept any value (since we don't have exhaustive list of all ItemKind variants)
       - But could validate against common ones: function, struct, enum, trait, type, const, static, macro

    5. Validate visibility values:
       - Accept: pub, pub(crate), pub(super), pub(in path), private
       - Reject invalid values with list of valid options

    6. Call validate() at the start of execute() and return any validation errors.

    Error messages should follow the style from CONTEXT.md:
    - Clear, actionable
    - Suggest how to fix
    - Reference --help-filters for more info
  </action>
  <verify>
    cargo check --lib
    cargo test --lib cli::expand
  </verify>
  <done>
    validate() method detects --include + --only conflict, detects contradictory patterns, validates visibility values, and returns helpful error messages. Called at start of execute().
  </done>
</task>

<task type="auto">
  <name>Task 2: Add FILTERING section to --help</name>
  <files>src/main.rs</files>
  <action>
    Enhance the CLI help output with filter documentation:

    1. Update the #[command(long_about = "...")] for the Query command to include a FILTERING section with 3-4 real examples:

    ```
    FILTERING:
        Filter results using glob patterns and criteria. Multiple filters combine with AND logic.
        Multiple values for the same flag combine with OR logic.

        --include "std::*"              Show only items from std crate
        --exclude "*::test*"            Exclude items with "test" in path
        --kind function                 Show only functions
        --only "serde::*"               Show only serde items (shorthand)
        --include "std::*" --kind fn    Show only std functions (AND logic)

    Examples:
        cargo doc-query query Vec --include "std::*"
        cargo doc-query query Error --exclude "*test*" --kind struct
        cargo doc-query query Serialize --only "serde::*"
    ```

    2. Add per-flag help text that explains:
       - --include: "Include items matching glob pattern. Multiple allowed (OR logic)."
       - --exclude: "Exclude items matching glob pattern. Multiple allowed (OR logic)."
       - --kind: "Filter by item kind (struct, enum, trait, function, etc.). Case-insensitive."
       - --crate: "Filter by crate name. Multiple allowed."
       - --visibility: "Filter by visibility: pub, pub(crate), pub(super), pub(in path), private"
       - --only: "Include only items matching pattern (mutually exclusive with --include)"

    3. Ensure help text mentions that --help-filters shows detailed glob syntax.

    Use the #[arg(help = "...")] attribute for each flag.
  </action>
  <verify>
    cargo build --release
    ./target/release/cargo-doc-query query --help | head -60
  </verify>
  <done>
    --help output includes FILTERING section with 3-4 real examples, each flag has descriptive help text, and --help-filters is mentioned as reference.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement --help-filters flag</name>
  <files>src/main.rs, src/types/filter.rs</files>
  <action>
    Create --help-filters documentation:

    1. Add a new subcommand or flag:
       - Option A: Add `cargo doc-query help-filters` subcommand
       - Option B: Add `--help-filters` flag to Query command that shows help and exits
       - RECOMMENDED: Option B (more discoverable)

    2. For Option B implementation in src/main.rs:
       - Add `help_filters: bool` field to Commands::Query
       - At start of Query match arm, check if help_filters is true
       - If true, print glob syntax guide and return Ok(())

    3. The glob syntax guide should include:
       - Header: "Filter Pattern Syntax (Glob Patterns)"
       - Syntax table:
         *       Matches any sequence of characters (except path separator)
         ?       Matches any single character
         **     Matches any sequence including path separators
         [...]  Matches any character in brackets
         [!...] Matches any character NOT in brackets
       - Examples section:
         'std::*'           → All items in std crate
         'std::vec::*'      → All items in std::vec module
         '*::test*'         → Items with "test" in the name
         '**::Display'      → Display trait anywhere
         'crate::[A-Z]*'    → Items starting with capital letter
         'serde::de::*'     → All items in serde::de module
       - Tips section:
         - Use quotes around patterns with special characters
         - Patterns are case-sensitive for paths
         - Multiple --include flags = OR logic
         - Different flag types = AND logic
       - Reference to glob crate documentation

    4. Store the help text as a constant or use FilterEngine::glob_syntax_help() with enhancements.
  </action>
  <verify>
    cargo build --release
    ./target/release/cargo-doc-query query --help-filters
    ./target/release/cargo-doc-query query --help | grep -i "help-filters"
  </verify>
  <done>
    --help-filters flag displays comprehensive glob syntax documentation with examples, syntax table, and tips. Command exits successfully after displaying help.
  </done>
</task>

<task type="auto">
  <name>Task 4: Enhance error messages for invalid patterns</name>
  <files>src/cli/expand.rs, src/types/filter.rs</files>
  <action>
    Improve error messages when pattern compilation fails:

    1. In src/cli/expand.rs execute() method, enhance FilterError handling:
       - For InvalidGlob { pattern, message }:
         ```
         Error: Invalid glob pattern '[invalid'

         The pattern '[invalid' is not valid: ...

         Example of a valid pattern: 'std::vec::*'

         For glob syntax reference, run: cargo doc-query query --help-filters
         ```

       - For EmptyPattern:
         ```
         Error: Empty filter pattern

         Filter patterns cannot be empty. Check your --include, --exclude, or --only flags.

         Example: --include "std::*"
         ```

    2. In src/types/filter.rs, ensure FilterError messages include the pattern when available.

    3. Add pattern context to error messages:
       - Show which flag had the error (--include, --exclude, etc.)
       - Show the actual value that failed
       - Provide a correct example
       - Reference --help-filters

    4. Test with actual invalid patterns to verify messages are helpful:
       - `[invalid` - unclosed bracket
       - `**` alone - valid but very broad (maybe warn?)
       - Empty string from shell parsing issues

    Error message structure:
    1. What went wrong
    2. Why (brief explanation)
    3. Example of correct usage
    4. Where to get more help
  </action>
  <verify>
    cargo build --release
    ./target/release/cargo-doc-query query Vec --include "[invalid" 2>&1 | head -10
    ./target/release/cargo-doc-query query Vec --include "" 2>&1 | head -10
    ./target/release/cargo-doc-query query Vec --include "std::*" --only "serde::*" 2>&1 | head -5
  </verify>
  <done>
    Invalid patterns show specific error with: the failing pattern, explanation, valid example, and reference to --help-filters. Conflicting flags show clear error with suggestion to fix.
  </done>
</task>

</tasks>

<verification>
1. Validation works: `--include "x" --only "y"` shows error about mutual exclusivity
2. Help text updated: `cargo doc-query query --help` shows FILTERING section with examples
3. Glob help works: `cargo doc-query query --help-filters` shows syntax guide
4. Error messages helpful: Invalid pattern shows what was wrong + example + reference
5. Visibility validation: Invalid visibility shows list of valid options
6. Kind case handling: `--kind FUNCTION` works (case-insensitive)
</verification>

<success_criteria>
- --include and --only mutual exclusivity enforced with clear error
- Contradictory patterns detected and reported
- Visibility values validated against known options
- --help output includes FILTERING section with 3-4 real examples
- --help-filters flag displays comprehensive glob syntax documentation
- Invalid glob patterns produce helpful error messages with:
  - The specific pattern that failed
  - Why it's invalid
  - A valid example
  - Reference to --help-filters
- Case-insensitive kind matching implemented
- All existing tests pass
- New validation tests added and passing
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-integration/07-03-SUMMARY.md`
</output>
