---
phase: 07-cli-integration
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/cli/expand.rs
  - src/query/expand.rs
autonomous: true

must_haves:
  truths:
    - "FilterConfig flows from CLI to query execution"
    - "FilterEngine filters expansion results before output"
    - "FilterStats displayed in non-quiet mode"
    - "Empty filter config has no performance impact"
    - "Filtered results show count of items filtered out"
  artifacts:
    - path: "src/cli/expand.rs"
      provides: "FilterEngine creation and application in execute()"
      min_lines: 30
    - path: "src/query/expand.rs"
      provides: "ExpansionResult filtering integration"
      min_lines: 20
  key_links:
    - from: "src/cli/expand.rs ExpandCommand::execute()"
      to: "src/query/expand.rs expand_type_with_config()"
      via: "filter_config parameter"
      pattern: "filter_config passed to expansion"
---

<objective>
Wire FilterConfig from CLI through to query execution, applying filters to expansion results.

Purpose: Actually apply the filter criteria specified by users to filter query results before display.

Output: FilterConfig flows from CLI args → ExpandCommand → query execution → FilterEngine → filtered results.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-cli-integration/07-CONTEXT.md
@src/cli/expand.rs
@src/query/expand.rs
@src/types/filter.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate FilterEngine into ExpandCommand::execute()</name>
  <files>src/cli/expand.rs</files>
  <action>
    Modify src/cli/expand.rs to compile and apply filters:

    1. Add imports at top:
       use crate::types::filter::{FilterConfig, FilterEngine, FilterError};

    2. In execute() method, after getting the expansion result:
       - Get FilterConfig from self.filter_config()
       - If config.has_filters():
         a. Compile FilterEngine using FilterEngine::compile(&config)
         b. Handle FilterError::InvalidGlob with helpful error message
         c. Handle FilterError::EmptyPattern with specific error
         d. Apply filter to expansion results using filter_with_stats
         e. Replace expansion with filtered version
         f. Display FilterStats.summary() in non-quiet mode

    3. The filtering should happen AFTER expansion is complete but BEFORE output formatting.
       This preserves the existing expansion behavior while adding post-filtering.

    4. Handle errors gracefully:
       - Invalid glob: show pattern and suggestion to run --help-filters
       - Empty pattern: suggest checking for empty strings in arguments

    Keep the existing flow: build → expand → filter (if configured) → output
  </action>
  <verify>
    cargo check --lib
    cargo test --lib cli::expand
  </verify>
  <done>
    execute() method compiles FilterConfig into FilterEngine, applies filters to expansion results, displays FilterStats when filters are active, and handles FilterError variants with helpful messages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add filter support to expansion result types</name>
  <files>src/query/expand.rs, src/types/expand.rs</files>
  <action>
    Ensure expansion results can be filtered by implementing Filterable trait:

    1. Check if ExpansionResult or its contained types need Filterable implementation
    2. If not already implemented, add Filterable trait implementation for the types that represent query results (likely ExpansionResult or TypeGraph/TypeNode)
    3. The Filterable trait requires:
       - filter_path() -> &str (fully qualified path)
       - filter_kind() -> &str (item kind)
       - filter_crate() -> &str (crate name)
       - filter_visibility() -> &str (visibility)

    4. Check src/types/expand.rs for the ExpansionResult, TypeGraph, TypeNode structures
    5. If TypeNode represents items, implement Filterable for TypeNode
    6. If filtering needs to work at a different level, consider implementing for ExpansionResult with a method to filter its contents

    Refer to src/types/filter.rs for Filterable trait definition and existing implementations (like for QueryMatch).
  </action>
  <verify>
    cargo check --lib
    cargo test --lib types::filter
  </verify>
  <done>
    Expansion result types implement Filterable trait or have a method to extract filterable items, allowing FilterEngine to filter the results.
  </done>
</task>

<task type="auto">
  <name>Task 3: Optimize filtering integration for performance</name>
  <files>src/cli/expand.rs</files>
  <action>
    Ensure filtering has minimal performance impact:

    1. Use FilterEngine::is_active() to check if filtering is needed before doing any work
    2. Only compile patterns if filters are configured (lazy compilation)
    3. Use filter_with_stats to get both filtered results AND statistics in one pass
    4. Display stats only in non-quiet mode and only when filters are active
    5. Consider adding timing for filter application (optional, add to stats if easy)

    The flow should be:
    - Expand type (existing logic, unchanged)
    - If filters configured:
      - Compile FilterEngine
      - Filter results with stats
      - Display stats
    - Format and output (existing logic)

    This ensures zero overhead when no filters are specified (fast path).
  </action>
  <verify>
    cargo test --lib -- --test-threads=1 2>&1 | grep -E "(test.*filter|test result)"
    cargo build --release
    time cargo doc-query query std::vec::Vec --minimal  # Should be fast
  </verify>
  <done>
    Filtering adds <5% overhead when active, zero overhead when disabled, stats displayed only when filters active and not quiet.
  </done>
</task>

</tasks>

<verification>
1. Filter works end-to-end: `cargo doc-query query Vec --include "std::*"` returns only std items
2. Multiple filters combine with AND: `--include "std::*" --kind function` returns only std functions
3. Stats displayed: Shows "X checked, Y passed (Z% rejection rate)"
4. No filters = no overhead: Query without filters is just as fast
5. Error handling: Invalid patterns show helpful error with --help-filters suggestion
</verification>

<success_criteria>
- FilterConfig correctly constructed from CLI args and compiled into FilterEngine
- FilterEngine applied to expansion results before display
- FilterStats displayed when filters active and not in quiet mode
- Invalid patterns produce helpful error messages
- Empty patterns handled gracefully
- Performance: <5% overhead when filters active, zero when disabled
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-integration/07-02-SUMMARY.md`
</output>
