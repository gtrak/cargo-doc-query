---
phase: 07-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.rs
  - src/cli/expand.rs
autonomous: true

must_haves:
  truths:
    - "CLI accepts --include/-i flag with glob patterns"
    - "CLI accepts --exclude/-e flag with glob patterns"
    - "CLI accepts --kind/-k flag with item kinds"
    - "CLI accepts --crate flag with crate names"
    - "CLI accepts --visibility flag with visibility levels"
    - "CLI accepts --only flag as shorthand for include+exclude"
    - "Multiple instances of same flag collect into Vec"
    - "Case-insensitive matching for --kind values"
  artifacts:
    - path: "src/main.rs"
      provides: "Filter flag definitions in Commands::Query variant"
      min_lines: 20
    - path: "src/cli/expand.rs"
      provides: "FilterConfig field in ExpandCommand struct"
      min_lines: 10
  key_links:
    - from: "src/main.rs Commands::Query"
      to: "src/cli/expand.rs ExpandCommand"
      via: "flag forwarding"
      pattern: "from_args call with filter params"
---

<objective>
Add filter CLI flags to the Query command and ExpandCommand struct.

Purpose: Enable users to specify filter criteria via command-line flags that will be passed to the FilterEngine.

Output: Updated CLI definitions with all filter flags properly typed and documented.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-cli-integration/07-CONTEXT.md
@src/main.rs
@src/cli/expand.rs
@src/types/filter.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter flags to Commands::Query enum variant</name>
  <files>src/main.rs</files>
  <action>
    Modify the Commands::Query variant in src/main.rs to add filter flags following the user decisions from CONTEXT.md:

    1. Add `-i, --include <PATTERN>`: multiple allowed, glob patterns for item paths
    2. Add `-e, --exclude <PATTERN>`: multiple allowed, glob patterns to filter out
    3. Add `-k, --kind <KIND>`: multiple allowed, item kind filter (struct, enum, trait, function, etc.)
    4. Add `--crate <CRATE>`: multiple allowed, restrict to specific crate(s)
    5. Add `--visibility <VIS>`: multiple allowed, visibility level (pub, pub(crate), pub(super), pub(in path), private)
    6. Add `--only <PATTERN>`: shorthand for "include this and exclude everything else", mutually exclusive with --include

    Use clap's Vec<String> for multiple values. Add proper doc comments with examples.
    For --kind, document that values are case-insensitive (function, Function, fn all work).
    For --visibility, list valid values in doc comment.
    For --only, add doc comment explaining it's mutually exclusive with --include.

    Update the match arm for Commands::Query to pass these values to ExpandCommand::from_args.
  </action>
  <verify>
    cargo check --lib
    cargo check --bin cargo-doc-query
  </verify>
  <done>
    Commands::Query variant has all 6 filter flags defined with proper types (Vec<String> for multiple values), short flags where specified (-i, -e, -k), and compile passes without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add FilterConfig to ExpandCommand struct</name>
  <files>src/cli/expand.rs</files>
  <action>
    Modify src/cli/expand.rs to integrate FilterConfig:

    1. Add `use crate::types::filter::FilterConfig;` at the top

    2. Add fields to ExpandCommand struct:
       - include: Vec<String>
       - exclude: Vec<String>
       - kind: Vec<String>
       - crate_filter: Vec<String> (don't use 'crate' as field name, it's a keyword)
       - visibility: Vec<String>
       - only: Option<String>

    3. Update ExpandCommand::from_args() to accept these new parameters

    4. Add a method `fn filter_config(&self) -> FilterConfig` that constructs and returns a FilterConfig from the stored fields. Handle the --only logic: if only is Some, create config with that as include and ignore include field; otherwise use include field.

    5. Update the new() constructor if it exists to handle the new fields (or mark it deprecated since from_args is the main entry point now).

    Keep all existing fields (path, depth, crate_name, tokens, minimal, json, quiet) unchanged.
  </action>
  <verify>
    cargo check --lib
    cargo test --lib cli::expand --no-run
  </verify>
  <done>
    ExpandCommand has all filter fields, from_args accepts filter parameters, filter_config() method returns properly constructed FilterConfig, and code compiles.
  </done>
</task>

</tasks>

<verification>
1. Filter flags appear in --help output: `cargo doc-query query --help | grep -E "(include|exclude|kind|visibility|only)"`
2. All flags accept multiple values: `--include "std::*" --include "serde::*"`
3. ExpandCommand can be constructed with filter args and produces valid FilterConfig
4. Existing functionality preserved: `--depth`, `--minimal`, `--tokens` still work
</verification>

<success_criteria>
- Commands::Query has 6 new filter flag options with proper clap attributes
- ExpandCommand stores filter configuration in struct fields
- filter_config() method correctly constructs FilterConfig from CLI args
- --only logic properly excludes everything except the pattern
- All code compiles and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-integration/07-01-SUMMARY.md`
</output>
