---
phase: 09-unified-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/format/mod.rs
  - src/format/item.rs
autonomous: true

must_haves:
  truths:
    - "Single format_item() dispatcher handles all 24 ItemKind variants"
    - "Consistent formatting rules regardless of item position"
    - "DetailLevel controls what metadata to display"
  artifacts:
    - path: "src/format/item.rs"
      provides: "format_item() dispatcher for all ItemKind variants"
      exports: ["ItemFormatter", "format_item", "FormattedItem"]
    - path: "src/format/mod.rs"
      provides: "Module exports for format package"
      contains: "pub mod item"
  key_links:
    - from: "src/format/item.rs"
      to: "src/types/detail.rs"
      via: "DetailLevel import and usage"
      pattern: "use.*DetailLevel"
    - from: "src/format/item.rs"
      to: "rustdoc_types"
      via: "ItemKind enum variants"
      pattern: "ItemKind::"
---

<objective>
Create unified format_item() dispatcher that handles all 24 ItemKind variants with consistent formatting rules.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-unified-rendering/09-RESEARCH.md
@.planning/phases/09-unified-rendering/09-CONTEXT.md
@src/types/detail.rs
@src/format/text.rs
</context>

<tasks>

<task type="auto">
  <name>Create format_item() dispatcher in src/format/item.rs</name>
  <files>src/format/item.rs</files>
  <action>
Create new file src/format/item.rs with:

1. Import types: DetailLevel from crate::types::detail, Item/ItemKind from rustdoc_types
2. Define FormattedItem struct with fields: id, kind, name, signature, visibility, generics, docs, fields, variants, items, is_deprecated, attributes, modifiers
3. Define ItemFormatter struct with: detail_level: DetailLevel, token_budget: Option<usize>
4. Implement ItemFormatter::new(detail_level, token_budget) -> Self
5. Implement format_item(&mut self, item: &Item) -> FormattedItem dispatcher:
   - Extract common fields: id, name, kind, visibility (if detail_level includes_visibility)
   - Extract generics (if detail_level includes_generics)
   - Extract docs (if not minimal) - get from item.docs field
   - Use match on item.kind to delegate to specific format_* functions
   - Handle all 24 ItemKind variants: Module, Struct, Enum, Union, Trait, Function, TypeAlias, Constant, Static, Macro, Use, Import, Exported, Reexport, StructField, Variant, TraitItem, ImplItem, ForeignType, ExternCrate, TyAlias, OpaqueTy, Static(async), Constant(trait)
   - Each format_* function extracts kind-specific data
6. Implement format_module, format_struct, format_enum, format_trait, format_function, format_type_alias, format_constant, format_static, format_macro with consistent structure
7. Return FormattedItem with all extracted data

Base format on existing patterns in src/format/text.rs but unify all ItemKind handling.
  </action>
  <verify>Create the file and verify: grep -n "pub fn format_item" src/format/item.rs shows the dispatcher</verify>
  <done>format_item() dispatcher exists and handles all ItemKind variants with DetailLevel control</done>
</task>

<task type="auto">
  <name>Update format/mod.rs to export new module</name>
  <files>src/format/mod.rs</files>
  <action>
Update src/format/mod.rs to add:
```rust
pub mod item;
```
</action>
  <verify>grep "pub mod item" src/format/mod.rs</verify>
  <done>format module exports item module</done>
</task>

<task type="auto">
  <name>Add tests for format_item dispatcher</name>
  <files>src/format/item.rs</files>
  <action>
Add #[cfg(test)] module at end of src/format/item.rs with tests:
1. test_format_item_struct - create Item with Struct variant, verify FormattedItem populated
2. test_format_item_function - create Item with Function variant, verify signature extracted
3. test_format_item_enum - create Item with Enum variant, verify variants extracted
4. test_format_item_respects_detail_level - verify Minimal omits visibility/generics
5. test_format_item_all_kinds - test at least 5 different ItemKind variants

Use existing Item constructors from rustdoc_types in test code.
  </action>
  <verify>cargo test format::item</verify>
  <done>Tests pass for format_item dispatcher</done>
</task>

</tasks>

<verification>
- [ ] src/format/item.rs exists with format_item() dispatcher
- [ ] All 24 ItemKind variants handled in match statement
- [ ] DetailLevel controls what metadata included
- [ ] Tests pass for dispatcher
</verification>

<success_criteria>
Single format_item() function handles all ItemKind variants with consistent formatting rules, controlled by DetailLevel
</success_criteria>

<output>
After completion, create `.planning/phases/09-unified-rendering/09-01-SUMMARY.md`
</output>
