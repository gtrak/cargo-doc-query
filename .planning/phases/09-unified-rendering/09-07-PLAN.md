---
phase: 09-unified-rendering
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/format/item.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Doc comments displayed in standard output mode (DOCS-02)"
    - "Token budget enforcement includes doc comment tokens (DOCS-07)"
  artifacts:
    - path: "src/format/item.rs"
      provides: "FormattedItem::render() outputs docs field"
      contains: "self.docs"
  key_links:
    - from: "src/format/text.rs"
      to: "FormattedItem::render()"
      via: "formatted.render()"
      pattern: "\\.render\\(\\)"
---

<objective>
Close remaining verification gaps from 09-VERIFICATION.md:
1. FormattedItem.render() doesn't output docs field (DOCS-02)
2. Token budget enforcement can't track docs because they're not rendered (DOCS-07)

Purpose: Complete DOCS-02 and DOCS-07 requirements
Output: Doc comments displayed in standard mode with working token budget
</objective>

<context>
@.planning/phases/09-unified-rendering/09-CONTEXT.md
@.planning/phases/09-unified-rendering/09-VERIFICATION.md

Reference for existing patterns:
- src/format/item.rs: FormattedItem struct and render() method (lines 15-86)
- src/format/doc.rs: DocHandler for doc formatting/truncation patterns

Key insight from VERIFICATION.md:
- FormattedItem has docs: Option<String> field (line 29)
- render() method (lines 49-86) outputs kind, id, generics, visibility, is_deprecated, fields, variants
- render() does NOT output: docs, signature, items, attributes, deprecation_note, modifiers
- This is why DOCS-02 fails: docs field exists but never rendered
- DOCS-07 fails as consequence: token tracking works but docs never displayed
</context>

<tasks>

<task type="auto">
  <name>Update render() to output docs field</name>
  <files>src/format/item.rs</files>
  <action>
1. Read src/format/item.rs to find the render() method (lines 49-86)
2. Add doc comment output to render() method after visibility section (around line 68)
3. Add:

   ```rust
   // Documentation
   if let Some(ref docs) = self.docs {
       let doc_lines: Vec<&str> = docs.lines().collect();
       for line in doc_lines {
           output.push_str(&format!("  {}\n", line));
       }
   }
   ```

4. Consider: Should docs be truncated for token budget? The text.rs already handles truncation via BudgetTracker, but render() just outputs what's in FormattedItem. Since expand command uses Standard detail level (not Minimal), docs should display.

Note: The DocHandler.truncate_docs() is already applied in text.rs when building FormattedItem. This task just adds the output of the docs field to render().

Verify after: cargo build passes, render() now outputs docs if present
  </action>
  <verify>
1. cargo build passes
2. render() method now includes self.docs output
3. Existing tests pass
  </verify>
  <done>render() outputs docs field when present, completing DOCS-02 requirement</done>
</task>

</tasks>

<verification>
After implementation:
1. cargo test passes
2. Check render() method includes docs output
3. Verify DOCS-02 and DOCS-07 now satisfied
</verification>

<success_criteria>
- [ ] render() outputs docs field when self.docs is Some
- [ ] DOCS-02: Doc comments display in standard output mode
- [ ] DOCS-07: Token budget enforcement includes doc comment tokens
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-unified-rendering/09-07-SUMMARY.md`
</output>
