---
phase: 09-unified-rendering
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/expand.rs
  - src/format/text.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Doc comments displayed with token budget truncation in expand command"
    - "Full ItemFormatter rendering pipeline used by CLI"
  artifacts:
    - path: "src/types/expand.rs"
      provides: "TypeNode with docs field"
      contains: "docs: Option<String>"
    - path: "src/format/text.rs"
      provides: "Doc-aware expand result formatting"
      contains: "formatted.docs"
  key_links:
    - from: "src/types/expand.rs"
      to: "item.docs"
      via: "builder method population"
      pattern: "\\.with_docs\\("
---

<objective>
Close verification gaps from 09-VERIFICATION.md:
1. Add docs field to TypeNode and populate from rustdoc Item::docs
2. Use ItemFormatter for rendering instead of manual println!

Purpose: Complete DOCS-01..07 requirements for expand command
Output: Working doc comment display with token budget in CLI
</objective>

<context>
@.planning/phases/09-unified-rendering/09-CONTEXT.md
@.planning/phases/09-unified-rendering/09-VERIFICATION.md

Reference for existing patterns:
- src/types/expand.rs: TypeNode struct (lines 110-160)
- src/format/item.rs: ItemFormatter and FormattedItem
- src/format/doc.rs: DocHandler::format_docs for truncation
</context>

<tasks>

<task type="auto">
  <name>Add docs field to TypeNode</name>
  <files>src/types/expand.rs</files>
  <action>
1. Read src/types/expand.rs to find the TypeNode struct definition (around lines 110-160)
2. Add field: `docs: Option<String>` to TypeNode struct
3. Find builder methods like `with_crate_visibility` (lines 362-389) 
4. Add new builder method `with_docs` that extracts `item.docs.as_ref().map(|s| s.trim().to_string())` and sets the docs field
5. Update `to_minimal()` method to clear the docs field
6. Add docs field to any other relevant TypeNode variants if they exist

Reference: item.rs line 109 shows how to extract docs: `item.docs.as_ref().map(|s| s.trim().to_string())`
  </action>
  <verify>
1. cargo build passes
2. TypeNode struct now has docs: Option<String> field
3. Builder method exists to populate docs from Item
  </verify>
  <done>TypeNode has docs: Option<String> field with builder method to populate from rustdoc Item::docs</done>
</task>

<task type="auto">
  <name>Wire docs into format_expand_result_with_formatter</name>
  <files>src/format/text.rs</files>
  <action>
1. Read src/format/text.rs around line 482 where FormattedItem is built
2. Change `docs: None` to extract docs from the original TypeNode: `docs: node.docs.clone()`
3. This enables doc token tracking at line 525 since docs will no longer be None
4. Verify doc_tokens calculation now works: `formatted.docs.as_ref().map(|d| d.len() / 4).unwrap_or(0)`

The key insight from VERIFICATION.md: TypeNode.docs was always None because the field didn't exist. Now that TypeNode has docs, we clone it into FormattedItem.
  </action>
  <verify>
1. cargo build passes
2. format_expand_result_with_formatter now passes docs to FormattedItem
3. Doc token tracking at line 525 will now work (not always 0)
  </verify>
  <done>FormattedItem receives docs from TypeNode, enabling token budget tracking for docs</done>
</task>

<task type="auto">
  <name>Use ItemFormatter for rendering (optional polish)</name>
  <files>src/format/text.rs</files>
  <action>
Read src/format/text.rs lines 529-550 where format_expand_result_with_formatter manually prints with println!.

Currently it builds a proper FormattedItem but then manually prints fields. Instead:
1. Import ItemFormatter if not already imported
2. Create ItemFormatter instance
3. Call ItemFormatter::format_item(&formatted) to get the rendered string
4. Use that for output instead of manual println! calls

Alternatively: Add a `render()` method to FormattedItem that uses ItemFormatter internally.

This ensures consistent rendering across all code paths.
  </action>
  <verify>
1. cargo build passes
2. Manual println! calls replaced with ItemFormatter call
3. Output remains functionally equivalent
  </verify>
  <done>CLI uses ItemFormatter rendering pipeline for consistency</done>
</task>

</tasks>

<verification>
After implementation:
1. Run cargo test to verify no regressions
2. Test: cargo doc-query expand <crate> --tokens 500 should show doc comments (if docs exist)
3. Verify doc_tokens calculation is no longer always 0
</verification>

<success_criteria>
- [ ] TypeNode has docs: Option<String> field
- [ ] TypeNode builder populates docs from item.docs
- [ ] format_expand_result_with_formatter passes docs to FormattedItem
- [ ] Doc token tracking works (not always 0)
- [ ] ItemFormatter used for rendering (or FormattedItem has render method)
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-unified-rendering/09-06-SUMMARY.md`
</output>
