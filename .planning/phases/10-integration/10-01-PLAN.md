---
phase: 10-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/mod.rs
  - tests/integration/filter_integration.rs
  - tests/integration/error_paths.rs
  - tests/integration/feature_combinations.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can run complex filtered queries: --include + --kind + --tokens work together"
    - "User sees helpful error messages for invalid glob patterns"
    - "User sees helpful error messages for empty result sets"
    - "User sees helpful error messages for conflicting flags"
    - "User can use filters + depth + tokens + minimal in combination"
    - "User sees consistent JSON output with new fields"
  artifacts:
    - path: "tests/integration/filter_integration.rs"
      provides: "Filter combination tests with CLI invocation"
    - path: "tests/integration/error_paths.rs"
      provides: "Error path validation tests"
    - path: "tests/integration/feature_combinations.rs"
      provides: "Multi-feature integration tests"
  key_links:
    - from: "tests/integration"
      to: "src/types/filter.rs"
      via: "CLI invocation with filter flags"
      pattern: "cargo doc-query query.*--include.*--kind"
    - from: "tests/integration"
      to: "src/cli/args.rs"
      via: "CLI argument parsing"
      pattern: "cargo doc-query query --help-filters"
---

<objective>
Create integration tests for filter combinations, error paths, and feature combinations.

Purpose: Validate all v1.1 features work together correctly through end-to-end tests
Output: Integration test files covering filter + depth, filter + token budget, filter + minimal mode, and error scenarios
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-filter-engine/06-01-SUMMARY.md
@.planning/phases/09-unified-rendering/09-01-SUMMARY.md

# Understanding what's implemented:
# - FilterEngine (src/types/filter.rs): FilterConfig, FilterEngine with glob matching
# - CLI flags (src/cli/args.rs): --include, --exclude, --kind, --crate, --visibility, --only
# - DetailLevel (src/types/detail.rs): Minimal/Standard/Detailed modes
# - Token budget (src/format/budget.rs): BudgetTracker for rendering
# - Doc handler (src/format/doc.rs): DocHandler with truncation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test directory structure</name>
  <files>tests/integration/mod.rs</files>
  <action>
Create tests/integration/mod.rs with basic test infrastructure. Use std::process::Command to invoke cargo doc-query as integration tests.

Create tests/integration/filter_integration.rs with tests for:
- Filter + depth combination: `cargo doc-query query Vec --include "std::*" --depth 2`
- Filter + token budget: `cargo doc-query query Vec --kind "function" --tokens 500`
- Filter + minimal mode: `cargo doc-query query Vec --exclude "*test*" --minimal`
- All three combined: `cargo doc-query query HashMap --include "std::*" --kind "struct" --depth 1 --tokens 300`

Use assert! to verify commands succeed (exit code 0) and produce non-empty output.
  </action>
  <verify>Run `cargo test --test integration` - tests should compile and execute</verify>
  <done>Integration test file exists, tests compile, basic filter combos work</done>
</task>

<task type="auto">
  <name>Task 2: Create error path tests</name>
  <files>tests/integration/error_paths.rs</files>
  <action>
Create tests/integration/error_paths.rs with tests for all error scenarios:

1. Invalid glob patterns: `cargo doc-query query Vec --include "[invalid"`
   - Should return non-zero exit code with helpful error message

2. Empty result sets: `cargo doc-query query NonexistentType`
   - Should return exit code 3 (Query returned no results)

3. Conflicting flags: `cargo doc-query query Vec --include "std::*" --only "serde::*"`
   - Should return error about mutually exclusive flags

4. Invalid crate name: `cargo doc-query query Vec --crate "nonexistent_crate_12345"`
   - Should handle gracefully (empty results or helpful message)

5. Invalid visibility filter: `cargo doc-query query Vec --visibility "invalid_visibility"`
   - Should validate and show available options

Use assert! for exit codes and assert!(output.contains(...)) for error messages.
  </action>
  <verify>Run `cargo test --test integration error_` - error path tests pass</verify>
  <done>All error scenarios produce appropriate exit codes and helpful messages</done>
</task>

<task type="auto">
  <name>Task 3: Create feature combination tests</name>
  <files>tests/integration/feature_combinations.rs</files>
  <action>
Create tests/integration/feature_combinations.rs testing all v1.1 features working together:

1. Filters + token budget + depth: Test complex queries like `Vec --include "std::*" --kind "fn" --depth 1 --tokens 500`
2. Filters + minimal mode: Test `--include "serde::*" --minimal` produces minimal output
3. Filters + detailed mode: Test `--kind "trait" --detailed` shows all metadata
4. All output formats: Test both text (default) and JSON (`--json`) produce valid output
5. Backward compatibility: JSON output includes new fields but doesn't break existing parsing

Test with serde, clap, glob crates (they should already be built from dev dependencies).
Verify JSON output is parseable and contains expected fields (visibility, generics, deprecation).
  </action>
  <verify>Run `cargo test --test integration feature_` - all combination tests pass</verify>
  <done>All feature combinations work correctly, JSON is valid and backward compatible</done>
</task>

</tasks>

<verification>
Run full integration test suite:
- `cargo test --test integration` should have all tests passing
- Verify tests use actual CLI invocation (std::process::Command)
- Verify test coverage includes filter combos, error paths, and JSON output
</verification>

<success_criteria>
1. Integration tests exist in tests/integration/ directory
2. Filter + depth + token budget + minimal combinations all work via CLI
3. Error scenarios (invalid glob, empty results, conflicting flags) produce helpful messages
4. JSON output is valid and backward compatible
5. Tests invoke actual cargo doc-query binary, not unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration/10-01-SUMMARY.md`
</output>
