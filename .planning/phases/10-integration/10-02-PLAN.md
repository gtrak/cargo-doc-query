---
phase: 10-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can query real crates (serde, clap, glob, petgraph) with filters"
    - "User sees consistent output with snapshot tests"
    - "User sees updated README with all v1.1 features documented"
    - "User sees comprehensive --help output with filter examples"
  artifacts:
    - path: "tests/snapshots/filter_output.txt"
      provides: "Golden files for snapshot testing"
    - path: "README.md"
      provides: "Project documentation with v1.1 features"
    - path: "src/cli/args.rs"
      provides: "Updated --help with filter examples"
  key_links:
    - from: "tests/snapshots"
      to: "tests/integration"
      via: "insta crate for snapshot assertions"
      pattern: "insta::assert_snapshot"
---

<objective>
Create end-to-end tests with real crates and update documentation.

Purpose: Validate with real crates and document all v1.1 features for users
Output: Snapshot tests with real crates, updated README and --help
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-integration/10-01-PLAN.md
@.planning/phases/09-unified-rendering/09-02-SUMMARY.md

# Test crates per CONTEXT.md:
# - Use crates cargo-doc-query depends on: serde, clap, glob, petgraph
# - Target ~10 crates total for integration testing
# - Snapshot testing approach per locked decision
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insta snapshot testing dependency</name>
<files>Cargo.toml</files>
  <action>
Add `insta` crate to dev-dependencies in Cargo.toml for snapshot testing:
```toml
[dev-dependencies]
insta = { version = "1.40", features = ["yaml", "toml"] }
```

This enables snapshot testing with golden file verification.
  </action>
  <verify>Run `cargo check --tests` - compiles with new dependency</verify>
  <done>insta added to dev-dependencies, compiles successfully</done>
</task>

<task type="auto">
<name>Task 2: Create snapshot tests with real crates</name>
<files>tests/snapshots/serde_query.rs, tests/snapshots/clap_query.rs</files>
  <action>
Create tests/snapshots/ directory with snapshot tests for real crates:

1. tests/snapshots/serde_query.rs - Query serde::Serialize with filters:
   - Basic query: `cargo doc-query query serde::Serialize`
   - With filter: `cargo doc-query query serde::Serialize --kind "struct"`
   - With tokens: `cargo doc-query query serde::Serialize --tokens 300`
   - JSON output: `cargo doc-query query serde::Serialize --json`

2. tests/snapshots/clap_query.rs - Query clap::Clap or similar
   - Test with nested types: `cargo doc-query query clap::App --depth 1`

3. tests/snapshots/glob_query.rs - Query glob patterns

4. tests/snapshots/petgraph_query.rs - Query petgraph for complex generics

Use insta::assert_snapshot! macro to capture and verify output.
Create .snapshots/ directory for golden files (add to .gitignore).
  </action>
<verify>Run `cargo test --test snapshots` - tests create/update snapshots</verify>
  <done>Snapshot tests exist, create golden files on first run, verify on subsequent runs</done>
</task>

<task type="auto">
<name>Task 3: Update README with v1.1 features</name>
<files>README.md</files>
  <action>
Create comprehensive README.md documenting all v1.1 features:

1. Quick Start section (existing)
2. Filter Flags section:
   - --include, --exclude with glob patterns
   - --kind for item type filtering
   - --crate for crate filtering
   - --visibility for visibility filtering
   - --only for exclusive matching
   - --help-filters for syntax help

3. Detail Level section:
   - --minimal for signatures only
   - --detailed for full metadata
   - Default behavior

4. Token Budget section:
   - --tokens for LLM context management
   - How truncation works

5. Depth section:
   - --depth for nested type expansion

6. Output Formats section:
   - Text (default)
   - JSON (--json flag)
   - JSON backward compatibility notes

7. Examples section with realistic queries

Keep it concise but complete - users should be able to discover all features.
  </action>
  <verify>README.md exists, contains all v1.1 feature sections, renders correctly</verify>
  <done>README.md documents all v1.1 features</done>
</task>

<task type="auto">
  <name>Task 4: Verify --help output is complete</name>
  <files>src/cli/args.rs</files>
  <action>
Run `cargo doc-query query --help` and verify:
1. All filter flags are documented (--include, --exclude, --kind, --crate, --visibility, --only)
2. Detail level flags are documented (--minimal, --detailed)
3. Token budget is documented (--tokens)
4. Depth flag is documented (--depth)
5. Output format flag is documented (--json)
6. FILTERING section includes working examples

If any sections are missing or incomplete, add appropriate doc comments to args.rs.
  </action>
  <verify>Run `cargo doc-query query --help | head -100` - all features documented</verify>
  <done>--help output shows all v1.1 features with examples</done>
</task>

</tasks>

<verification>
- `cargo test --test snapshots` passes with snapshot assertions
- README.md exists with all v1.1 feature sections
- `cargo doc-query query --help` shows comprehensive documentation
- .snapshots directory exists with golden files
</verification>

<success_criteria>
1. Snapshot tests verify output consistency for real crate queries
2. README documents all v1.1 features clearly
3. --help output is comprehensive and includes examples
4. Tests use insta for snapshot assertions
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration/10-02-SUMMARY.md`
</output>
