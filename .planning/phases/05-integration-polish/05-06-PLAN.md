---
phase: 05-integration-polish
plan: 06
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/cli/expand.rs
  - src/cli/query.rs
  - src/query/suggest.rs
  - src/format/text.rs
autonomous: true

must_haves:
  truths:
    - "When query returns no results, type suggestions are displayed"
    - "Suggestions show similar crate names when query fails"
    - "Both 'query' and 'expand' subcommands show suggestions"
  artifacts:
    - path: "src/cli/expand.rs"
      provides: "ExpandCommand with suggestion logic"
      min_lines: 200
      exports: ["ExpandCommand", "ExpandOptions"]
    - path: "src/format/text.rs"
      provides: "Suggestion display formatting"
      min_lines: 50
      exports: ["format_suggestions"]
    - path: "src/query/suggest.rs"
      provides: "Suggestion generation logic"
      min_lines: 100
  key_links:
    - from: "src/cli/expand.rs"
      to: "src/query/suggest.rs"
      via: "import and use"
      pattern: "suggest::find_similar"
    - from: "src/cli/expand.rs"
      to: "src/format/text.rs"
      via: "import and call"
      pattern: "format_suggestions"
---

<objective>
Fix type suggestions not showing by moving suggestion logic from QueryCommand to ExpandCommand so both code paths display "Did you mean:" suggestions when queries fail.

Purpose: The suggestion logic was implemented in QueryCommand.execute() but the query subcommand now delegates to ExpandCommand, bypassing the suggestion logic. This gap closure moves the suggestion display to ExpandCommand where it will be triggered for both paths.

Output: Updated CLI handlers with unified suggestion display in ExpandCommand
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-polish/05-UAT.md
@.planning/phases/05-integration-polish/05-05-SUMMARY.md

**Gap Reference:**
- truth: "Type suggestions show when query returns no results"
- status: failed
- reason: "Type suggestions implemented in QueryCommand.execute() but query subcommand now delegates to ExpandCommand which bypasses the suggestion logic"
- severity: major
- test: 10
- missing:
  - "Move suggestion logic from QueryCommand to ExpandCommand"
  - "Ensure suggestions display when expansion fails with 'No items found'"

@src/cli/query.rs
@src/cli/expand.rs
@src/query/suggest.rs
@src/format/text.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor suggestion logic into reusable components</name>
  <files>src/format/text.rs, src/query/suggest.rs</files>
  <action>
    1. In src/format/text.rs: Add a new public function `format_suggestions(suggestions: &[String]) -> String` that formats the "Did you mean:" message with properly indented, colorized suggestions
    
    2. Ensure the function returns a string that can be printed (not print directly) so it can be used flexibly
    
    3. In src/query/suggest.rs: Verify `find_similar(query: &str, index: &Index) -> Vec<String>` exists and returns similar type names from the index
    
    4. If suggest.rs doesn't exist or lacks the function, create it with a simple fuzzy matching algorithm that:
       - Finds types with similar names using edit distance
       - Returns top 3-5 suggestions
       - Handles case-insensitive matching
  </action>
  <verify>
    Run `cargo check` to verify modules compile correctly
  </verify>
  <done>
    - format_suggestions function exists in text.rs and returns formatted string
    - suggest::find_similar function exists and compiles
    - Both modules compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate suggestions into ExpandCommand</name>
  <files>src/cli/expand.rs</files>
  <action>
    1. In src/cli/expand.rs, locate the Execute implementation for ExpandCommand
    
    2. Find where the command handles empty results ("No items found" or similar message)
    
    3. Add suggestion logic:
       - Before returning "No items found" result, call suggest::find_similar() with the query path and loaded index
       - If suggestions are found, append formatted suggestions to the error/output
       - Use format_suggestions() from text.rs to format the output
    
    4. Ensure suggestions are only shown when:
       - No items are found in the expansion
       - Suggestions exist in the index
       - The query wasn't an exact match
    
    5. Update any imports needed at the top of expand.rs:
       - Add `use crate::query::suggest;`
       - Add `use crate::format::text::format_suggestions;` (if not already present)
    
    6. Ensure the suggestion output matches the expected format:
       ```
       No results found for "anywhow"
       
       Did you mean:
         • anyhow
         • anyhow::Error
       ```
  </action>
  <verify>
    Run `cargo build` to verify the project compiles successfully
  </verify>
  <done>
    - ExpandCommand.execute() includes suggestion logic
    - Suggestions are generated and formatted when no items found
    - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update QueryCommand to remove duplicate suggestion logic</name>
  <files>src/cli/query.rs</files>
  <action>
    1. In src/cli/query.rs, locate the Execute implementation
    
    2. Since QueryCommand now delegates to ExpandCommand, ensure:
       - The delegation properly passes through to ExpandCommand
       - Any duplicate suggestion logic in QueryCommand is removed (to avoid double suggestions)
       - The delegation preserves all necessary options and flags
    
    3. If QueryCommand has its own suggestion logic that would duplicate ExpandCommand's:
       - Remove the duplicate logic
       - Keep only the delegation to ExpandCommand
       - Ensure error handling still works correctly
    
    4. Verify the query.rs imports are clean and no unused suggestion imports remain
  </action>
  <verify>
    Run `cargo build` and `cargo clippy` to verify clean compilation
  </verify>
  <done>
    - QueryCommand delegates cleanly to ExpandCommand
    - No duplicate suggestion logic exists
    - Clean compilation with no warnings
  </done>
</task>

</tasks>

<verification>
**Build Verification:**
- [ ] `cargo build` completes without errors
- [ ] `cargo clippy` shows no warnings

**Functional Verification:**
- [ ] Build cache with a real crate: `cargo run -- build`
- [ ] Test with typo: `cargo run -- query anywhow` should show "Did you mean: anyhow"
- [ ] Test expand with typo: `cargo run -- expand anywhow` should show suggestions
- [ ] Test with exact match: `cargo run -- query anyhow` should NOT show suggestions
- [ ] Test with nonexistent: `cargo run -- query xyz123` should show "No results" (may or may not have suggestions)
</verification>

<success_criteria>
1. Type suggestions display when query/expand returns no results
2. Both `query` and `expand` subcommands show "Did you mean:" suggestions
3. Suggestions use proper formatting with bullets/indents
4. No duplicate suggestion logic exists between commands
5. Code compiles without warnings
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-polish/05-06-SUMMARY.md`
</output>
