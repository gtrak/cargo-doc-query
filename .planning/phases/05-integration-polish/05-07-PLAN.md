---
phase: 05-integration-polish
plan: 07
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/cache/store.rs
autonomous: true

must_haves:
  truths:
    - "Corrupt cache files trigger auto-rebuild with warning"
    - "Postcard deserialization errors return None instead of Err"
    - "Auto-rebuild logic is triggered for corrupt cache"
  artifacts:
    - path: "src/cache/store.rs"
      provides: "CacheStore with proper error handling"
      min_lines: 150
      exports: ["CacheStore", "CacheStore::load"]
  key_links:
    - from: "CacheStore::load"
      to: "auto-rebuild logic"
      via: "return None on deserialize error"
      pattern: "Err(_) => Ok(None)"
---

<objective>
Fix corrupt cache detection so that deserialization failures return None instead of Err, triggering the auto-rebuild logic with a helpful warning message.

Purpose: Currently CacheStore.load() returns an error on deserialization failure, which bypasses the auto-rebuild logic. This gap closure catches postcard deserialization errors and treats them as "cache not found" (None), allowing the system to automatically rebuild.

Output: Updated CacheStore with proper error handling for corrupt cache files
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-polish/05-UAT.md

**Gap Reference:**
- truth: "Corrupt cache shows warning and auto-rebuilds"
- status: failed
- reason: "CacheStore.load() returns error on deserialization failure instead of None, bypassing auto-rebuild logic"
- severity: minor
- test: 11
- missing:
  - "Fix CacheStore.load() to catch postcard errors and return None instead of Err"

@src/cache/store.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify CacheStore.load() error handling</name>
  <files>src/cache/store.rs</files>
  <action>
    1. Open src/cache/store.rs and locate the `load()` method on `CacheStore`
    
    2. Find the current postcard deserialization code (likely using `postcard::from_bytes` or similar)
    
    3. Wrap the deserialization in a match or if-let that catches postcard errors:
       - If deserialization succeeds: return Ok(Some(index))
       - If deserialization fails (postcard error): 
         * Log/print a warning: "Cache file appears corrupt, will rebuild..."
         * Return Ok(None) to trigger auto-rebuild
    
    4. The fix should look approximately like:
       ```rust
       match postcard::from_bytes(&data) {
           Ok(index) => Ok(Some(index)),
           Err(e) => {
               eprintln!("Warning: Cache file appears corrupt ({}), will rebuild...", e);
               Ok(None)
           }
       }
       ```
    
    5. Ensure the function signature remains the same:
       - Should still return `Result<Option<Index>, CacheError>` or similar
       - Only the error case behavior changes (Err -> Ok(None) with warning)
    
    6. Check if there's an existing file existence check before deserialization:
       - Keep the file-not-found case as Ok(None) (no warning needed)
       - Only show warning for deserialization failures on existing files
  </action>
  <verify>
    Run `cargo build` to verify the code compiles
  </verify>
  <done>
    - CacheStore.load() catches postcard deserialization errors
    - Returns Ok(None) instead of Err on deserialize failure
    - Shows "cache corrupt" warning before returning None
    - File-not-found case still returns Ok(None) silently
    - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify auto-rebuild integration</name>
  <files>src/cache/store.rs</files>
  <action>
    1. Review how CacheStore.load() is called elsewhere in the codebase
    
    2. Verify that callers handle Ok(None) by triggering auto-rebuild:
       - Look in src/cli/query.rs or similar for the pattern:
         ```rust
         match cache.load() {
             Ok(Some(index)) => use_index(index),
             Ok(None) => { /* auto-rebuild */ }
             Err(e) => { /* handle error */ }
         }
         ```
       - Ensure Ok(None) triggers the auto-rebuild path
    
    3. If callers expect different behavior, verify they still work:
       - The change from Err to Ok(None) should be transparent to most callers
       - Callers that were handling Err for corrupt cache should now see Ok(None)
    
    4. Verify the warning message is user-friendly:
       - Clear that cache is corrupt
       - Indicates automatic rebuild will happen
       - Not overly technical (hide postcard internals if shown)
  </action>
  <verify>
    Run `cargo clippy` to check for any code issues
    Review caller code to ensure integration is correct
  </verify>
  <done>
    - All callers handle Ok(None) correctly
    - Auto-rebuild path is triggered for corrupt cache
    - Warning message is clear and helpful
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Test corrupt cache handling</name>
  <what-built>
    Corrupt cache detection that returns None instead of Err, triggering auto-rebuild with a warning
  </what-built>
  <how-to-verify>
    1. First, build the cache normally:
       ```bash
       cargo run -- build
       ```
    
    2. Find the cache file location (usually in `target/doc-query/` or similar)
    
    3. Corrupt the cache file:
       ```bash
       # Example: truncate or modify the cache file
       echo "corrupted data" > /path/to/cache.bin
       # Or use: head -c 100 /path/to/cache.bin > /tmp/corrupt.bin && mv /tmp/corrupt.bin /path/to/cache.bin
       ```
    
    4. Run a query and observe the output:
       ```bash
       cargo run -- query anyhow
       ```
       
       Expected output:
       ```
       Warning: Cache file appears corrupt, will rebuild...
       [rebuild happens]
       [query results appear]
       ```
    
    5. Verify the cache was rebuilt and query succeeded
  </how-to-verify>
  <resume-signal>Type "approved" if corrupt cache shows warning and auto-rebuilds successfully</resume-signal>
</task>

</tasks>

<verification>
**Build Verification:**
- [ ] `cargo build` completes without errors
- [ ] `cargo clippy` shows no warnings

**Code Review:**
- [ ] CacheStore.load() catches postcard::from_bytes errors
- [ ] Returns Ok(None) on deserialize failure (not Err)
- [ ] Shows warning message: "Cache file appears corrupt, will rebuild..."
- [ ] File not found still returns Ok(None) without warning
- [ ] All callers handle Ok(None) properly
</verification>

<success_criteria>
1. CacheStore.load() returns Ok(None) on deserialization errors
2. Corrupt cache shows warning message "Cache file appears corrupt, will rebuild..."
3. Auto-rebuild logic is triggered automatically
4. Clean cache files still load normally
5. Missing cache files still return None silently (no false warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-polish/05-07-SUMMARY.md`
</output>
