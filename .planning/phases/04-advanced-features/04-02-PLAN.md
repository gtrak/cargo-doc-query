---
phase: 04-advanced-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/expand.rs
  - src/types/expand.rs
  - src/query/expand.rs
autonomous: true

must_haves:
  truths:
    - "User can limit output size with --tokens flag"
    - "User can get signature-only output with --minimal flag"
    - "Token budget prevents context overflow in LLM workflows"
    - "Minimal mode reduces output size by 50%+ vs full output"
  artifacts:
    - path: "src/cli/expand.rs"
      provides: "ExpandCommand with --tokens and --minimal flags"
      contains: ["tokens:", "minimal"]
    - path: "src/types/expand.rs"
      provides: "Token counting and minimal output support"
      contains: ["token_count", "to_minimal"]
    - path: "src/query/expand.rs"
      provides: "Budget-aware expansion"
      contains: ["token_budget", "is_minimal"]
  key_links:
    - from: "ExpandCommand flags"
      to: "TypeExpander config"
      via: "constructor parameters"
      pattern: "TypeExpander::new_with_options"
    - from: "TypeExpander"
      to: "token counting"
      via: "estimated_token_count() method"
      pattern: "serde_json::to_string"
---

<objective>
Add token budget constraints and minimal output mode to the expand command for LLM-efficient API exploration.

Purpose: Enable users to control context window usage when exploring types for LLM consumption. Critical for staying within token limits when preparing prompts for code analysis.

Output: Working `--tokens N` and `--minimal` flags that constrain and reduce output size.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/query.rs
@src/types/expand.rs (from 04-01)
@src/cli/expand.rs (from 04-01)
@src/query/expand.rs (from 04-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token counting infrastructure</name>
  <files>src/types/expand.rs</files>
  <action>
    Extend src/types/expand.rs with token counting:
    
    1. Add `token_count` field to TypeGraph:
       - token_count: usize (estimated tokens in this graph)
       - budget_exceeded: bool (true if expansion stopped due to token limit)
    
    2. Implement TokenCount trait or methods:
       - `impl TypeGraph { fn estimate_tokens(&self) -> usize }`
       - Rough estimate: serialize to JSON string, split on whitespace/punctuation, count tokens
       - OR simpler: total string length / 4 (average 4 chars per token)
       - Update token_count after each node addition
    
    3. Add BudgetExceeded error/info variant:
       - When expansion stops due to token limit, set budget_exceeded = true
       - Add truncated_nodes: Vec<String> listing which types weren't expanded
    
    4. Add minimal representation methods:
       - `TypeNode::to_minimal(&self) -> MinimalTypeNode`
       - MinimalTypeNode contains: id, kind, signature only (no docs, no field details)
       - `impl TypeGraph { fn to_minimal(&self) -> Self }` - converts full graph to minimal
    
    5. Add TokenConfig struct:
       - budget: Option<usize> (None = unlimited)
       - minimal_mode: bool
       - warning_threshold: f32 (e.g., 0.8 = warn at 80% of budget)
  </action>
  <verify>cargo check passes, TypeGraph has token_count and to_minimal() method</verify>
  <done>Token counting and minimal conversion infrastructure in place</done>
</task>

<task type="auto">
  <name>Task 2: Implement budget-aware expansion</name>
  <files>src/query/expand.rs</files>
  <action>
    Extend TypeExpander in src/query/expand.rs with budget awareness:
    
    1. Add configuration fields to TypeExpander:
       - token_budget: Option<usize>
       - minimal_mode: bool
       - current_token_count: usize
       - truncated: Vec<String>
    
    2. Update constructor:
       - new_with_options(engine, depth_limit, token_budget, minimal_mode) -> Self
       - Keep existing new() as convenience wrapper with None/unlimited defaults
    
    3. Implement token checking during expansion:
       - Before adding each node, estimate its token cost
       - If cost would exceed budget: mark as truncated, skip, continue
       - Track cumulative token count
       - When budget hit: set budget_exceeded flag, stop expanding new nodes
       - Still complete current node (don't leave partial data)
    
    4. Implement minimal mode:
       - When minimal_mode = true, create TypeNode with reduced fields:
         - Keep: id, kind, generic_params
         - Omit: docs, field details (just count of fields), source location
         - For methods: signature only, no docs or implementation details
       - Or simpler: build full graph, then convert to minimal before returning
       - Ensure minimal reduces size by ~50% for typical types
    
    5. Add warnings:
       - If approaching budget threshold (80%), emit warning to stderr
       - If budget exceeded, list truncated types in output
    
    6. Update ExpansionResult to include:
       - total_tokens: usize
       - budget_exceeded: bool
       - truncated_paths: Vec<String>
  </action>
  <verify>cargo check passes, TypeExpander tracks token_budget and minimal_mode</verify>
  <done>Budget-aware expansion with token tracking and minimal mode support</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI flags for token budgets</name>
  <files>src/cli/expand.rs</files>
  <action>
    Update ExpandCommand in src/cli/expand.rs with new flags:
    
    1. Add CLI arguments:
       ```rust
       /// Maximum tokens in output (approximate, default: unlimited)
       #[arg(long)]
       tokens: Option<usize>,
       
       /// Output minimal representation (signatures only, no docs)
       #[arg(long)]
       minimal: bool,
       ```
    
    2. Update execute() method:
       - Parse tokens and minimal flags
       - Pass to TypeExpander::new_with_options()
       - After expansion, check result.budget_exceeded
       - If exceeded: print warning to stderr listing truncated types
       - Print token count to stderr (like timing info in QueryCommand)
    
    3. Add validation:
       - --tokens must be >= 100 if provided (sanity check)
       - Error if tokens < 100: "Token budget too small, minimum is 100"
    
    4. Update help text to explain:
       - What "tokens" means (approximate LLM token count)
       - What minimal mode includes/excludes
       - Examples of typical token counts for common types
    
    5. Test integration:
       - Ensure flags are passed through to TypeExpander
       - Verify output structure matches expected JSON schema
  </action>
  <verify>
    cargo run -- expand --help shows --tokens and --minimal flags
    cargo run -- expand anyhow::Error --tokens 500 --minimal works
    cargo run -- expand anyhow::Error --minimal outputs smaller JSON than without flag
  </verify>
  <done>ExpandCommand supports --tokens N and --minimal flags with proper validation</done>
</task>

</tasks>

<verification>
1. Build passes: `cargo build`
2. Flags work: `--tokens 500` and `--minimal` accepted
3. Token limiting: Test with small budget (--tokens 200) to verify truncation
4. Minimal mode: Compare output size with/without --minimal (should be ~50% smaller)
5. Budget warnings: Verify warnings printed when approaching/exceeding limit
6. JSON validity: All output is valid JSON even when truncated
</verification>

<success_criteria>
- `cargo doc-query expand anyhow::Error --tokens 1000` stops expansion at ~1000 tokens
- `cargo doc-query expand anyhow::Error --minimal` outputs signatures only (no docs)
- Using both flags: `--minimal --tokens 500` provides maximum efficiency
- Output includes token_count field showing actual tokens used
- When budget exceeded, truncated_paths lists types that weren't fully expanded
- Typical reduction: minimal mode cuts output size by 50%+ vs full mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-02-SUMMARY.md`
</output>
