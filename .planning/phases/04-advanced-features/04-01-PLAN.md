---
phase: 04-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/expand.rs
  - src/query/expand.rs
  - src/types/expand.rs
  - src/main.rs
  - src/cli/mod.rs
autonomous: true

must_haves:
  truths:
    - "User can run 'cargo doc-query expand <path>' to see full type hierarchy"
    - "Expansion respects --depth flag to limit recursion"
    - "Circular type references don't cause infinite loops"
    - "Output is valid JSON showing parent-child relationships"
  artifacts:
    - path: "src/cli/expand.rs"
      provides: "ExpandCommand with --depth flag"
      exports: ["ExpandCommand"]
    - path: "src/query/expand.rs"
      provides: "TypeExpander with cycle detection"
      exports: ["TypeExpander", "expand_type"]
    - path: "src/types/expand.rs"
      provides: "Expansion output types"
      exports: ["TypeGraph", "TypeNode", "ExpansionResult"]
  key_links:
    - from: "ExpandCommand"
      to: "TypeExpander"
      via: "execute() method"
      pattern: "TypeExpander::new()"
    - from: "TypeExpander"
      to: "cycle detection"
      via: "visited set of type IDs"
      pattern: "HashSet<Id>"
---

<objective>
Create recursive type expansion command that explores type hierarchies with depth limits and cycle detection.

Purpose: Enable users to understand complex type relationships by recursively expanding field types, generic parameters, and return types. Critical for LLM context preparation when exploring unfamiliar APIs.

Output: Working `cargo doc-query expand <path> --depth N` command with cycle-safe recursion.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli/query.rs
@src/query/engine.rs
@src/types/query.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expansion output types</name>
  <files>src/types/expand.rs</files>
  <action>
    Create src/types/expand.rs with expansion-specific output types:
    
    1. `TypeGraph` - Top-level expansion result containing:
       - root: String (the path that was expanded)
       - depth_limit: u32
       - nodes: Vec<TypeNode>
       - cycles_detected: Vec<String> (paths that hit cycle limits)
    
    2. `TypeNode` - Individual type in the graph:
       - id: String (fully qualified path)
       - kind: String (struct, enum, primitive, generic)
       - fields: Vec<FieldInfo> (for struct-like types)
       - variants: Vec<VariantInfo> (for enums)
       - generic_params: Vec<String>
       - depth: u32 (distance from root)
    
    3. `FieldInfo` - Struct/enum field:
       - name: String
       - type_path: String
       - is_optional: bool
       - nested_type_id: Option<String> (reference to another node)
    
    4. Implement Serialize for all types using serde.
    
    5. Add `impl TypeGraph` methods:
       - new(root: String, depth_limit: u32) -> Self
       - add_node(&mut self, node: TypeNode) -> String (returns node id)
       - add_cycle(&mut self, path: String)
    
    Add `pub mod expand;` to src/types.rs if not already present.
  </action>
  <verify>cargo check passes with no errors for types module</verify>
  <done>TypeGraph, TypeNode, FieldInfo types exist with Serialize impl and helper methods</done>
</task>

<task type="auto">
  <name>Task 2: Implement TypeExpander with cycle detection</name>
  <files>src/query/expand.rs</files>
  <action>
    Create src/query/expand.rs implementing recursive type expansion:
    
    1. `pub struct TypeExpander` with fields:
       - engine: QueryEngine (reuse existing)
       - visited: HashSet<Id> (cycle detection - rustdoc type IDs)
       - current_depth: u32
       - depth_limit: u32
    
    2. `impl TypeExpander` methods:
       - new(engine: QueryEngine, depth_limit: u32) -> Self
       - expand(&mut self, path: &str) -> Result<ExpansionResult>
       - expand_type_internal(&mut self, item_id: &Id, depth: u32) -> Result<Option<TypeNode>>
    
    3. Cycle detection logic:
       - Before processing any type, check if its Id is in visited set
       - If yes: return Ok(None) and record cycle in result
       - If no: add to visited, process, remove when backtracking (for siblings)
       - OR keep in visited for path-based tracking (simpler: once visited, always cycle)
    
    4. Type extraction logic:
       - Match on ItemEnum::Struct, Enum, TypeAlias, etc.
       - For Struct: extract StructKind fields (Plain, Tuple, Unit)
       - For each field: resolve type, create FieldInfo, queue nested expansion if depth < limit
       - For generics: capture GenericArgs as strings
    
    5. Helper: resolve_type_to_path(&Type) -> String:
       - Handle Type::ResolvedPath, Generic, Primitive, etc.
       - Format fully qualified paths
    
    6. Add `pub fn expand_type(path: &str, depth: u32) -> Result<ExpansionResult>` convenience function.
    
    Add `pub mod expand;` to src/query/mod.rs.
    
    Reference src/query/engine.rs for:
    - How to load crates from cache
    - How to resolve paths to Item IDs
    - How to access Item and ItemEnum
  </action>
  <verify>cargo check passes with no errors for query/expand module</verify>
  <done>TypeExpander struct with expand() method and cycle detection via visited HashSet</done>
</task>

<task type="auto">
  <name>Task 3: Create ExpandCommand CLI</name>
  <files>src/cli/expand.rs, src/main.rs, src/cli/mod.rs</files>
  <action>
    1. Create src/cli/expand.rs with ExpandCommand:
       
       #[derive(Parser, Debug)]
       pub struct ExpandCommand {
           /// The type path to expand (e.g., anyhow::Error)
           path: String,
           
           /// Maximum recursion depth (default: 3)
           #[arg(long, default_value = "3")]
           depth: u32,
           
           /// Limit to specific crate
           #[arg(long)]
           crate_name: Option<String>,
       }
       
       impl Command for ExpandCommand {
           fn execute(&self) -> Result<()> {
               // Check cache freshness (reuse logic from QueryCommand)
               // Load QueryEngine via QueryEngine::from_cache()
               // Create TypeExpander with depth limit
               // Call expand(self.path)
               // Serialize result to JSON and print to stdout
               // Print timing info to stderr (like QueryCommand does)
           }
       }
    
    2. Add `pub mod expand;` to src/cli/mod.rs
       Add `pub use expand::ExpandCommand;` to src/cli/mod.rs
    
    3. Add "expand" subcommand to main.rs:
       - Add ExpandCommand to Cli enum
       - Add match arm in main() to handle Commands::Expand
       
    4. Ensure manifest change detection logic is reused from QueryCommand:
       - Check if CacheKeyInputs matches current index
       - Rebuild if needed (or extract to shared helper)
       
    5. Add helpful error messages for:
       - Type not found (suggest checking path spelling)
       - No cache (suggest running build first)
       - Invalid depth (must be > 0, suggest --depth range)
  </action>
  <verify>
    cargo build passes
    cargo run -- expand --help shows the command
    cargo run -- expand anyhow::Error --depth 2 works (if cache exists)
  </verify>
  <done>cargo doc-query expand <path> --depth N executes and outputs JSON type graph</done>
</task>

</tasks>

<verification>
1. Build passes: `cargo build`
2. Help works: `cargo run -- expand --help` shows --depth, --crate options
3. Test expansion: `cargo run -- expand anyhow::Error --depth 2` returns valid JSON
4. Cycle detection: Test with recursive type (like LinkedList) to verify cycles_detected field
5. Depth limit: Verify --depth 1 shows only immediate fields, --depth 2 shows one level deeper
</verification>

<success_criteria>
- User can run `cargo doc-query expand std::option::Option --depth 2`
- Output is valid JSON with TypeGraph structure
- --depth flag limits recursion (depth 0 = just root type, depth 1 = root + fields, etc.)
- Circular references (e.g., Node { next: Box<Node> }) are detected and recorded
- Performance: Expansion completes in <1s for typical types at depth 3
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-01-SUMMARY.md`
</output>
