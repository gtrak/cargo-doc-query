---
phase: 04-advanced-features
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/types/query.rs
  - src/query/engine.rs
  - src/cli/query.rs
autonomous: true

must_haves:
  truths:
    - "Existing query command supports --minimal flag"
    - "Query output can be token-budget constrained"
    - "Query and expand commands share consistent flag behavior"
    - "All Phase 2 querying features work with Phase 4 output options"
  artifacts:
    - path: "src/cli/query.rs"
      provides: "QueryCommand with --minimal and --tokens flags"
      contains: ["minimal", "tokens"]
    - path: "src/types/query.rs"
      provides: "Minimal query output support"
      exports: ["MinimalMethodOutput", "to_minimal()"]
    - path: "src/query/engine.rs"
      provides: "Token-aware query execution"
      contains: ["token_budget", "minimal_mode"]
  key_links:
    - from: "QueryCommand flags"
      to: "QueryOptions"
      via: "parse_options() method"
      pattern: "QueryOptions::new()"
    - from: "QueryOptions"
      to: "QueryEngine"
      via: "execute_with_options()"
      pattern: "engine.query_with_options()"
---

<objective>
Integrate token budgets and minimal mode into the existing query command for consistency across all query operations.

Purpose: Ensure the original query command benefits from Phase 4's LLM-optimization features. Users should have consistent --minimal and --tokens flags whether using `query` or `expand` commands.

Output: Updated query command with --minimal and --tokens flags that work identically to expand command.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli/query.rs
@src/types/query.rs
@src/query/engine.rs
@src/types/expand.rs (reference for token counting pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal output to query types</name>
  <files>src/types/query.rs</files>
  <action>
    Extend src/types/query.rs with minimal representation support:
    
    1. Add `to_minimal()` methods to output types:
       - `impl MethodOutput { fn to_minimal(&self) -> Self }`
         - Keep: name, signature, return_type, is_public
         - Remove: docs, is_trait_method if false
       - `impl TraitImplOutput { fn to_minimal(&self) -> Self }`
         - Keep: trait_name, trait_path, methods (minimalized)
         - Remove: generic_args, provided_methods
       - `impl TypeResult { fn to_minimal(&self) -> Self }`
         - Keep: kind, methods (minimalized), trait_implementations (minimalized)
       - `impl TraitResult { fn to_minimal(&self) -> Self }`
         - Keep: name, path, methods (minimalized), associated_types
         - Remove: provided_methods
       - `impl QueryMatch { fn to_minimal(&self) -> Self }`
       - `impl QueryResponse { fn to_minimal(&self) -> Self }`
    
    2. Add token estimation:
       - `impl QueryResponse { fn estimate_tokens(&self) -> usize }`
       - Serialize to JSON, divide string length by 4 (rough token estimate)
    
    3. Add budget tracking fields (optional enhancement):
       - Add `#[serde(skip_serializing_if = "Option::is_none")]`
       - token_count: Option<usize>
       - budget_exceeded: Option<bool>
    
    4. Ensure backward compatibility:
       - All new fields must be optional or have defaults
       - Existing JSON output format should not change when flags not used
  </action>
  <verify>cargo check passes, QueryResponse has to_minimal() method</verify>
  <done>Query types support minimal conversion and token estimation</done>
</task>

<task type="auto">
  <name>Task 2: Update QueryOptions and QueryEngine</name>
  <files>src/query/engine.rs</files>
  <action>
    Extend QueryOptions and QueryEngine in src/query/engine.rs:
    
    1. Update `QueryOptions` struct (around line 15):
       ```rust
       pub struct QueryOptions {
           kind: QueryKind,
           include_docs: bool,
           include_private: bool,
           minimal_mode: bool,      // NEW
           token_budget: Option<usize>, // NEW
       }
       ```
    
    2. Update `QueryOptions::new()` to initialize new fields to defaults:
       - minimal_mode: false
       - token_budget: None
    
    3. Add builder methods:
       - `fn with_minimal(mut self, minimal: bool) -> Self`
       - `fn with_token_budget(mut self, budget: Option<usize>) -> Self`
    
    4. Update QueryEngine::query() method (or create query_with_options):
       - Accept &QueryOptions parameter (already done, just use new fields)
       - After building QueryResponse:
         - If options.minimal_mode: convert to minimal via response.to_minimal()
         - If options.token_budget.is_some():
           - Check estimate_tokens() against budget
           - If exceeded, truncate or return error
           - Add token_count to response
    
    5. Add token-aware truncation logic:
       - If budget exceeded, prioritize:
         1. Keep public methods only (drop private)
         2. Keep trait implementations but minimalize methods
         3. Drop docs first (already handled by include_docs)
       - Set budget_exceeded flag in response
  </action>
  <verify>cargo check passes, QueryOptions has minimal_mode and token_budget fields</verify>
  <done>QueryEngine respects minimal_mode and token_budget options</done>
</task>

<task type="auto">
  <name>Task 3: Add flags to QueryCommand CLI</name>
  <files>src/cli/query.rs</files>
  <action>
    Update QueryCommand in src/cli/query.rs:
    
    1. Add CLI arguments (after existing include arg, around line 32):
       ```rust
       /// Output minimal representation (signatures only, no docs)
       #[arg(long)]
       minimal: bool,
       
       /// Maximum tokens in output (approximate)
       #[arg(long)]
       tokens: Option<usize>,
       ```
    
    2. Update `parse_options()` method (around line 90):
       ```rust
       fn parse_options(&self) -> QueryOptions {
           // ... existing parsing ...
           
           QueryOptions::new(kind)
               .with_docs(include_docs)
               .with_private(include_private)
               .with_minimal(self.minimal)
               .with_token_budget(self.tokens)
       }
       ```
    
    3. Update execute() to handle budget exceeded:
       - After getting QueryResponse, check if budget_exceeded flag is set
       - If set: print warning to stderr
       - Print token count to stderr (similar to timing info)
       - List truncated items if available
    
    4. Add validation:
       - If tokens < 100, error with "Token budget too small, minimum is 100"
    
    5. Test backward compatibility:
       - Existing `cargo doc-query query anyhow::Error` should work unchanged
       - Output format should be identical when new flags not used
  </action>
  <verify>
    cargo run -- query --help shows --minimal and --tokens flags
    cargo run -- query anyhow::Error --minimal works
    cargo run -- query anyhow::Error --tokens 500 works
    Existing queries without flags work identically to before
  </verify>
  <done>QueryCommand supports --minimal and --tokens flags consistently with ExpandCommand</done>
</task>

</tasks>

<verification>
1. Build passes: `cargo build`
2. Help shows flags: `cargo run -- query --help` displays --minimal and --tokens
3. Backward compatible: `cargo run -- query anyhow::Error` works as before
4. Minimal works: `cargo run -- query anyhow::Error --minimal` outputs smaller JSON
5. Budget works: `cargo run -- query anyhow::Error --tokens 300` truncates if needed
6. Combined: `cargo run -- query anyhow::Error --minimal --tokens 200` works
7. Consistent: Output format matches expand command behavior
</verification>

<success_criteria>
- `cargo doc-query query anyhow::Error --minimal` outputs signature-only format
- `cargo doc-query query anyhow::Error --tokens 500` respects token limit
- Both flags work together: `--minimal --tokens 300`
- Output includes token_count showing actual usage
- Existing queries without new flags behave identically (backward compatible)
- Flag behavior is consistent between `query` and `expand` commands
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features/04-03-SUMMARY.md`
</output>
